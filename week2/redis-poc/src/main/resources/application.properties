spring.application.name=redis-poc

# Redis Master Configuration (for writes)
redis.master.host=localhost
redis.master.port=6379

# Redis Replica Configuration (for reads)  
redis.replica.host=localhost
redis.replica.port=6380
